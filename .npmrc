public-hoist-pattern[]=*@heroui/*
public-hoist-pattern[]=*@nextui-org/*
# Force npm to install all platform-specific optional binaries
# (lightningcss, swc, sharp) so CI on linux-x64-gnu can build
# even when the lockfile was generated on macOS.
optional=true
